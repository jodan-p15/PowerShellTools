<# Steps
1. Create logic to take input from local and remote ip fields
2. Take the results from step 1 and perform a name lookup
3. Takes names and correlate to their respective IP address

#>
$combinedIPAddresses = $communication.LocalIP + $communication.RemoteIP

$assets = $combinedIPAddresses | Select -Unique

$informationTable@()
Foreach ($asset in $assets){
   $results = Resolve-DNSName -Name $asset
   $informationTable += [PSCustomObject] {
      'HostName' = $results.<hostname>
      'DomainName' = $results.<domainname>
   }
}
return $informationTable


consolidatedResults @()